__pycache__/
*.pyc
*.egg-info/
node_modules/
.frappe_manager/

# Real database backups/dumps must never land in this open-source repo —
# they can carry real encrypted credentials and real business data. Kept
# entirely outside any project folder instead (see 2026-08-21 session).
*.sql
*.sql.gz
*backup*.gz
_backups/
